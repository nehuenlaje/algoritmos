#include <stdio.h>
#include <stdlib.h>


int main(int argc, char *argv[]) {
    int count = atoi(argv[1]);
    // int count = i;

    for (int i = 0;i<count;i++)
        printf("%s\n","hello world" );

}
